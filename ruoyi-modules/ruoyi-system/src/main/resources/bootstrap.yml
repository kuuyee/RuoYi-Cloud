# Tomcat
server:
  port: 9201

# Spring
spring: 
  application:
    # 应用名称
    name: gomk-system
  profiles:
    # 环境配置
    active: dev
  cloud:
    nacos:
      discovery:
        # 服务注册地址
        server-addr: 114.67.253.192:8848
      config:
        # 配置中心地址
        server-addr: 114.67.253.192:8848
        # 配置文件格式
        file-extension: yml
        namespace: 9232ad04-a160-405d-a759-7c8cc5ad10af
        # 共享配置
        shared-dataids: application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
